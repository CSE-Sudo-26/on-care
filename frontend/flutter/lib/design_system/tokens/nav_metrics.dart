/// 하단 내비게이션 바의 치수.
///
/// 셸이 바를 그릴 때 쓰는 숫자와, 바 위에 무언가를 놓는 쪽이 비켜야 할
/// 숫자는 **같은 값이어야 한다.** 예전에는 두 값이 셸 안에 상수로만 있어
/// 바깥에서는 `+` 버튼이 바 위로 얼마나 솟았는지 알 길이 없었고, 하단에 뜬
/// 알림이 그 경계에 걸쳤다(#1259).
class AppNavMetrics {
  AppNavMetrics._();

  /// 목적지 아이콘과 라벨이 들어가는 바 본체의 높이.
  static const double barHeight = 58;

  /// `+` 버튼이 바 위로 솟은 만큼. 바 위젯은 이 여백까지 자기 높이로
  /// 들고 있어서(투명), 바 위에 놓이는 것은 자동으로 `+` 를 비킨다.
  static const double addButtonLift = 24;

  /// `+` 버튼의 지름.
  static const double addButtonSize = 56;

  /// 바 아래 안전영역을 이만큼까지만 따른다. 그보다 깊은 기기에서도
  /// 바가 지나치게 두꺼워지지 않는다.
  ///
  /// 22 일 때 홈 인디케이터가 있는 기기(인셋 34)에서 라벨 아래가 인디케이터에
  /// 닿아 가려 보였고, 28 로도 실기에서 빠듯했다. 지금 값은 그런 기기의
  /// 인셋을 그대로 따른다 — 즉 이 상한은 그보다 깊은 기기에서만 걸린다.
  static const double maxBottomInset = 34;
}
