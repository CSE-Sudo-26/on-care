/// 홈 '오늘의 AI 통합 조언' 데모 문구 — 식단·운동·코치 피드백을 한 문장으로
/// 엮은 큐레이션 카피다.
///
/// 홈이 이 문구에 닿는 경로가 둘이라 한 곳에 모아 둔다:
///
/// 1. `MockDashboardRepository` — `useMockApi` 데모에서 홈이 실제로 읽는 값
/// 2. `seedIfEmpty` 의 `dashboard_ai_advice` KV — Dio + `LocalApiInterceptor`
///    경로에서 나트륨 급원 기반 동적 경고 대신 노출되는 값
///
/// 두 곳이 따로 문구를 들고 있다가, 데모 경로가 1번으로 바뀌자 홈에 다른
/// 문구가 나왔다. 다시 갈라지지 않게 상수 하나로 묶는다.
const String kDemoAiAdvice =
    '아침 식단과 저녁 PT는 완벽했습니다! 점심 짬뽕으로 높아진 나트륨과 혈당을 낮추기 위해 '
    '물을 충분히 마시고, 코치님이 강조하신 어깨 스트레칭으로 건강하게 마무리해 보세요.';
